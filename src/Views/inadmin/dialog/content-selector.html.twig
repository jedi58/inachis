{% if pages is not empty  %}
    {% for page in pages %}
        <li data-value="{{ page.id }}">
            <label for="page_{{ page.id }}">{{ page.title }}</label>
        </li>
    {% endfor %}
{% endif %}
